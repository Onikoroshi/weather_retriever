%h1 Listing weather_informations

%table
  %thead
    %tr
      %th Zip code
      %th Data
      %th
      %th
      %th

  %tbody
    - @weather_informations.each do |weather_information|
      %tr
        %td= weather_information.postal_code
        %td= weather_information.data
        %td= link_to 'Show', weather_information
        %td= link_to 'Edit', edit_weather_information_path(weather_information)
        %td= link_to 'Destroy', weather_information, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Weather information', new_weather_information_path
